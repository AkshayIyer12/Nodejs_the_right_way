const zmq = require('zeromq');
const cluster = require('cluster')
const numWorkers = require('os').cpus().length

const pusher = zmq.socket('push').bind('ipc://filer-sendJobs.ipc');
const puller = zmq.socket('pull').bind('ipc://filer-recJobs.ipc');

let readyWorkers = 0;

if (cluster.isMaster) {
  for (let i = 0; i < numWorkers; i++) {
    
}